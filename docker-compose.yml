cucumber-phantomjs:
  build: .
  command: bundle exec rake parallel_cucumber
  volumes:
    - .:/myapp
  ports:
    - "4777:4777"

cucumber-chome:
  build: .
  command: bundle exec rake chrome
  volumes:
    - .:/myapp

# cucumber-firefox:
#   build: .
#   command: bundle exec rake selenium
#   volumes:
#     - .:/myapp
#
# cucumber-saucelabs:
#   build: .
#   command: bundle exec rake sauce
#   volumes:
#     - .:/myapp
#
# cucumber-browserstack:
#   build: .
#   command: bundle exec rake browserstack
#   volumes:
#     - .:/myapp
#
# cucumber-appium:
#   build: .
#   command: bundle exec cucumber -p appium_android_web -f pretty
#   volumes:
#     - .:/myapp
#   ports:
#     - "4723:4723"
